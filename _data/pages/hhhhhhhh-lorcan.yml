id: 0417b464-3b00-4ee0-8295-f096181b24ae
title: HHHHHHHH Lorcan
template: content
breadcrumbs: true
page_last_updated: true
blocks:
  - type: markdown
    body: >-
      ## P﻿age with different code blocks


      T﻿his page has many examples 


      ### H﻿ere is some code in CSS


      ```css

      .main {
        font-size: 18px;
        letter-spacing: 1px;
      }

      ```


      ### H﻿ere is code example in Rust


      ```rust

      #[contract]

      mod CounterContract {
         #[external]
         fn increase_counter(amount: u128) { ... }
         #[external]
         fn decrease_counter(amount: u128) { ... }
         #[view]
         fn get_counter() -> u128 { ... }
      }

      ```


      ### C﻿ode in Cairo language


      ```cairo

      func find_element{range_check_ptr}(array_ptr: felt*, elm_size, n_elms, key) -> (elm_ptr: felt*) {
          alloc_locals;
          local index;
          %{
              ...
          %}
          assert_nn_le(a=index, b=n_elms - 1);
          tempvar elm_ptr = array_ptr + elm_size * index;
          assert [elm_ptr] = key;
          return (elm_ptr=elm_ptr);
      }

      ```
